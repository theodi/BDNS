---
execute:
  echo: false
tbl-cap-location: bottom
jupyter:
  jupytext:
    text_representation:
      extension: .qmd
      format_name: quarto
      format_version: '1.0'
      jupytext_version: 1.16.1
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

# Building Device and Asset Naming Standards initiative

Status: *release 1.1.0*

## Abbreviations Register

```{python}
import pandas as pd
from itables import init_notebook_mode, show

init_notebook_mode(all_interactive=True)
df = pd.read_csv("BDNS_Abbreviations_Register.csv")
show(df, buttons=["pageLength", "csvHtml5"], lengthMenu=[20, 50, 100, 500])
```
